# Outputs

This folder stores reusable project outputs.

Subfolders:

- `outputs/figures/` for PNG charts
- `outputs/maps/` for HTML maps
- `outputs/reports/` for text and Markdown model reports
- `outputs/tables/` for CSV forecast tables
