# Data

This folder stores project-specific data products exported locally.

Folder layout:

- `data/raw/` for downloaded or exported source tables
- `data/interim/` for cleaned intermediate tables
- `data/processed/` for final modeling tables

The main workflow is designed to pull source data directly from Google Earth Engine.
