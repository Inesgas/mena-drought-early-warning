# Notebooks

Main notebook:

- `mena_drought_early_warning_forecasting.ipynb`

This notebook runs the forecasting workflow with the reusable code under `src/`.

After a monthly grid CSV has been exported, `scripts/run_baseline_pipeline.py` runs the baseline workflow from the command line.
